{
  "name": "functions",
  "scripts": {
    "build": "rm -rf lib && tsc",
    "serve": "yarn build && firebase serve",
    "deploy": "firebase use staging && firebase deploy",
    "deploy:production": "firebase use production && firebase deploy"
  },
  "engines": {
    "node": "14"
  },
  "main": "lib/index.js",
  "dependencies": {
    "@sentry/node": "6.1.0",
    "firebase-admin": "9.5.0",
    "firebase-functions": "3.13.1"
  },
  "private": true
}
